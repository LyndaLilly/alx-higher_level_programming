#!/usr/bin/python3
from magic_calculation_102 import add, sub


def adds(a, b):
    if a < b:
        c = add(a, b)
        for m in range(4, 6):
            y = add(y, m)
        return (y)
    else:
        return sub(a, b)
